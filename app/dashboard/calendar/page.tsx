import { StubModule } from "@/components/stub-module";

export default function CalendarPage() {
  return (
    <StubModule
      title="Calendar & scheduling"
      subtitle="Unified release timelines, recurrence, conflict warnings, plus OCR ingestion when ready."
    />
  );
}
