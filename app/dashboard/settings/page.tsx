import { StubModule } from "@/components/stub-module";

export default function SettingsPage() {
  return (
    <StubModule
      title="Admin settings"
      subtitle="Tenant configuration, SSO policies, webhook secrets rotation, connector scopes—documented separately from integrations."
    />
  );
}
