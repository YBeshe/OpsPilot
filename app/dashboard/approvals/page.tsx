import { StubModule } from "@/components/stub-module";

export default function ApprovalsPage() {
  return (
    <StubModule
      title="Approvals"
      subtitle="Workflows will capture escalation paths, SLA timers, immutable sign-off artefacts, and executive summaries."
    />
  );
}
