import { StubModule } from "@/components/stub-module";

export default function SupportAssistantPage() {
  return (
    <StubModule
      title="AI support assistant"
      subtitle="Contextual chat over runbooks launches after SSO and knowledge embeddings are authoritative."
    />
  );
}
