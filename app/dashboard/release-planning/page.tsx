import { StubModule } from "@/components/stub-module";

export default function ReleasePlanningPage() {
  return (
    <StubModule
      title="Release planning"
      subtitle="Plans, milestones, task ownership, and risk telemetry will converge here alongside Jira lineage."
    />
  );
}
