import { StubModule } from "@/components/stub-module";

export default function AutomationsPage() {
  return (
    <StubModule
      title="Automation center"
      subtitle="Approved workflow recording, scripted steps, and guarded execution windows stay behind mature RBAC and audit parity."
    />
  );
}
