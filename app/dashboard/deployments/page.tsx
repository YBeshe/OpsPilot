import { StubModule } from "@/components/stub-module";

export default function DeploymentsPage() {
  return (
    <StubModule
      title="Deployments"
      subtitle="Promotion tracking, readiness scoring, environment health—all backed by audited integration calls."
    />
  );
}
