import { StubModule } from "@/components/stub-module";

export default function ValidationsPage() {
  return (
    <StubModule
      title="Validations"
      subtitle="Copado validation ingestion, parsers, dashboards, and AI-assisted triage hooks land in Phase 3+."
    />
  );
}
